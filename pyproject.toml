[project]
name = "grupos"
version = "0.1.0"
description = "Generador estático de listas de grupos"
requires-python = ">=3.12"
dependencies = [
  "jinja2>=3.1",
  "pyyaml>=6.0",
]

[tool.uv]
package = false
